export default function Contact({accent}) {
    return (
        <section className="py-24"
                 style={{
                     background: `linear-gradient(180deg,#070414, ${accent}10)`
                 }}
        id="contact">
            <h2>Contact</h2>
        </section>
    )
}